# 密钥文件：只存在于服务器/本机，绝不入库
profiles.json
profiles.json.bak.*
# 运行期产物
_jobs/
_cache/
usage.json
__pycache__/
tests/_tmp/

# 临时凭据（API token 等）：绝不入库
.secrets/
.tools/

# Cloudflare Worker 的本地产物
cf/.wrangler/
cf/.dev.vars
cf/build/

# wrangler dev 的本地产物（在仓库根目录跑时）
.wrangler/
